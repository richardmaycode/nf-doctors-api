<div class="table-row dark:text-slate-200">
  <div class="table-cell p-3">
    <%= employee.name %>
  </div>
  <div class="table-cell dark:text-slate-200">
    <%= employee.coordinator ? "Yes" : "No" %>
  </div>
  <div class="table-cell dark:text-slate-200">
    <%= employee.location.name %>
  </div>
  <div class="table-cell">
    <div class="flex gap-4">
      <%= link_to "View", facility_location_path(@facility), class: "dark:text-slate-200"%>
      <%= link_to "Edit", edit_facility_location_path(@facility), class: "dark:text-slate-200" %>
      <%= button_to "Delete", facility_location_path(@facility), data: { method: :delete }, class: "text-red-900" %>
    </div>
  </div>
</div>